<UserControl x:Class="Blackjack.View.MainWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="Red">
        <Button Content="New Game" Width="75" Height="20" Command="{Binding _StartGame}" Margin="112,140"/>
        <Button Content="Resume" Width="75" Height="20" Command="{Binding _StartGame}" Margin="112,165,112,115"/>
        <Button Content="Diffculty" Width="75" Height="20" Command="{Binding _StartGame}" Margin="112,190,112,90"/>
        <Button Content="End" Width="75" Height="20" Command="{Binding _StartGame}" Margin="112,215,112,65"/>
    </Grid>
</UserControl>
